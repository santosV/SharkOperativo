<Page
    x:Class="SharkOperativo.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SharkOperativo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Parte 1 titulo y regreso-->
        <Grid Background="WhiteSmoke">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Image Source="Assets/return.png"></Image>
            <TextBlock Margin="10,0,0,0" Grid.Column="1" Text="Lorem Inmsup" HorizontalAlignment="Left" VerticalAlignment="Center" FontFamily="Century Gothic" FontSize="20" Foreground="#FF0C0707"></TextBlock>
        </Grid>
        <!-- Termina parte 1 titulo y regreso-->
        
        <!--Parte 2-->
        <Grid Grid.Row="1" Background="AliceBlue">
            
        </Grid>
        
        <!-- Termina parte 2-->
        
    </Grid>
</Page>